console.log("Hello, Charlie! 🕵️‍♂️");

/**
 * Sample function to test our setup
 * @param name - Name to greet
 * @returns Greeting message
 */
export function greet(name: string): string {
  return `Hello, ${name}! Ready to analyze some git history?`;
}
