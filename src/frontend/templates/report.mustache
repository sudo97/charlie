<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{title}}</title>
    <style>{{css}}</style>
  </head>
  <body>
    <div id="app">
    </div>
    <div class="container">
      <h1>{{title}}</h1>
      <h2>File-level Hotspots</h2>
      <div id="visualization" class="visualization">
        <!-- D3 visualization will be inserted here -->
      </div>


      <h2>Coupled Pairs</h2>
      <div id="coupled-pairs" class="visualization">
      </div>

      <div id="coupled-pairs-table" class="visualization">
      </div>


      <div id="soc">
      </div>

      <hr/>
      
      <h1>Architectural Groups</h1>
      <p style="color: #666; font-size: 0.9em; margin: 20px 0;">
        Note: If no architectural groups were provided in the configuration, the grouped hotspots and coupled pairs visualizations below will look similar to the file-level graphs above, as files will not be aggregated into higher-level groups.
      </p>
      <h2>Grouped Hotspots</h2>
      <div id="grouped-hotspots" class="visualization">
      </div>

      <h2>Coupled Pairs Grouped</h2>
      <div id="coupled-pairs-grouped" class="visualization">
      </div>


      <h1>Word Count</h2>
      <p style="color: #666; font-size: 0.9em; margin: 20px 0;">
        Your team's modus operandi. Not really a metric, but if you see that FIX is the most common word by far, it should make you think.
      </p>
      <div id="word-count">
      </div>
    </div>

    <!-- Inject report data -->
    <script id="data" type="application/json">
      {{{reportDataJson}}}
    </script>

    <script id="grouped-hotspots-data" type="application/json">
      {{{groupedHotspotsJson}}}
    </script>

    <script id="coupled-pairs-data" type="application/json">
      {{{coupledPairsJson}}}
    </script>

    <script id="coupled-pairs-grouped-data" type="application/json">
      {{{coupledPairsGroupedJson}}}
    </script>

    <script id="soc-data" type="application/json">
      {{{socDataJson}}}
    </script>

    <script id="word-count-data" type="application/json">
      {{{wordCountJson}}}
    </script>
    
    <!-- Load the built frontend bundle -->
    <script type="module">
      {{{js}}}
    </script>
  </body>
</html> 